const { assert } = require('chai');
const { describe, it } = require('mocha');

function parseNumber(value) {
    return number;
}

describe('parseNumber', () => {
    it('should parse', () => {
        assert.equal(parseNumber('2 6 +'));
    });
});